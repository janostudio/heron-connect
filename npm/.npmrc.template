# npm publish auth — generated from this template by `make npm-auth`.
# Do NOT edit this file directly; it is the source template.
# Real .npmrc (with token) is gitignored and generated locally.
//registry.npmjs.org/:_authToken=<TOKEN>
